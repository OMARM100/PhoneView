# PhoneView scrcpy 4.1 patch

Pinned from Genymobile/scrcpy v4.1.

Modified files:
- `app/src/screen.c` — in-window PhoneView mapping editor and mapping dispatch.
- `app/src/screen.h` — PhoneView control state.

The remainder of the vendored client source is kept aligned with upstream v4.1.
The Android server is intentionally kept as the official matching v4.1 prebuilt server in the PhoneView installer.